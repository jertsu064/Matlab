r=input('input radius: ');
h=input('input height: ');
v=(1/3)*(pi*(r^2))*h;
fprintf('A right circular cone with a radius of %d', r);
fprintf(' cm and a height of %d', h);
fprintf(' cm, has a volume of %.2f', v);
fprintf(' cc\n');
